.venv/
data/
doc-src/_build_doc.html
doc-src/_build_dry_run.html
__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# Wi-Fi/OTA credentials — copy firmware/turn_counter/secrets.example.h to
# secrets.h and fill it in. Ignored before the file exists so it can never be
# staged by accident.
firmware/*/secrets.h

# arduino-cli build output for `make ota`
build/
